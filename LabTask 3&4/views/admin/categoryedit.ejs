<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SOFA STUDIO - Edit Category</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #fdf8e1; /* Cream background */
            font-family: 'Poppins', sans-serif;
            color: #333333;
        }

        .form-container {
            background-color: #fff;
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
            margin: 40px auto;
            max-width: 600px;
        }

        h4 {
            color: #c41e3a;
            font-family: 'Playfair Display', serif;
            font-weight: bold;
            text-align: center;
            margin-bottom: 20px;
        }

        label {
            font-weight: bold;
            color: #333333;
        }

        .form-control {
            border: 1px solid #e6d7b2;
            border-radius: 8px;
        }

        .form-control:focus {
            border-color: #c41e3a;
            box-shadow: 0 0 5px rgba(196, 30, 58, 0.2);
        }

        .btn-primary {
            background-color: #c41e3a;
            border: none;
            border-radius: 30px;
            font-weight: bold;
            color: #fff;
            transition: background-color 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #8c1428;
        }

        .form-text {
            color: #c41e3a;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg" style="background-color: #fff; border-bottom: 1px solid #e6d7b2;">
        <div class="container-fluid">
            <a class="navbar-brand" href="#" style="color: #c41e3a; font-family: 'Playfair Display', serif; font-weight: bold;">
                SOFA STUDIO
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
    </nav>

    <!-- Form Container -->
    <div class="form-container">
        <h4>Edit Category</h4>
        <form action="" method="post">
            <div class="mb-3">
                <label for="IDInput">Category ID</label>
                <input name="id" type="text" class="form-control" id="IDInput" placeholder="Enter Category ID">
                <div class="form-text">Unique identifier for the category.</div>
            </div>
            <div class="mb-3">
                <label for="titleInput">Title</label>
                <input name="title" type="text" class="form-control" id="titleInput" placeholder="Enter Title">
                <div class="form-text">Descriptive title for the category.</div>
            </div>
            <div class="mb-3">
                <label for="descriptionInput">Description</label>
                <textarea name="description" class="form-control" id="descriptionInput" placeholder="Enter Description" rows="3"></textarea>
                <div class="form-text">Provide detailed information about the category.</div>
            </div>
            <div class="text-center mt-4">
                <button type="submit" class="btn btn-primary px-5">Save Changes</button>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
