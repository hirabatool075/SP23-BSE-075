<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Product</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
        font-family: 'Poppins', sans-serif;
        background-color: #fdf8f0; /* Cream background */
        color: #5a5a5a;
        margin: 0;
        padding: 0;
    }

    h4 {
        text-align: center;
        color: #c62828; /* Deep red heading */
        font-size: 1.8rem;
        font-weight: bold;
        margin: 20px 0;
    }

    .admin-container {
        max-width: 700px;
        margin: 30px auto;
        padding: 25px 30px;
        background-color: #ffffff; /* White card */
        border: 2px solid #c62828; /* Red border */
        border-radius: 12px;
        box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
    }

    .admin-container:hover {
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    }

    .form-group {
        margin-bottom: 20px;
    }

    .form-group label {
        display: block;
        font-weight: 600;
        margin-bottom: 8px;
        color: #b71c1c; /* Dark red for labels */
        font-size: 1rem;
    }

    .form-control {
        width: 100%;
        padding: 12px 15px;
        border: 1px solid #e57373; /* Light red border */
        border-radius: 8px;
        font-size: 1rem;
        color: #333;
        background-color: #fffdf6; /* Subtle cream input */
        transition: border-color 0.3s ease, box-shadow 0.3s ease;
    }

    .form-control:focus {
        outline: none;
        border-color: #c62828; /* Red focus border */
        box-shadow: 0 0 8px rgba(198, 40, 40, 0.5);
        background-color: #ffffff;
    }

    .form-text {
        font-size: 0.85rem;
        color: #757575; /* Muted grey text */
    }

    select.form-control {
        background-color: #fffdf6; /* Subtle cream dropdown */
        cursor: pointer;
    }

    .btn-primary {
        display: block;
        width: 100%;
        padding: 12px;
        font-size: 1rem;
        font-weight: bold;
        background-color: #c62828; /* Bold red button */
        color: #fff;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .btn-primary:hover {
        background-color: #b71c1c; /* Darker red on hover */
        transform: translateY(-2px);
    }

    .btn-primary:active {
        background-color: #8e0000; /* Even darker red */
        transform: translateY(0);
    }
  </style>
</head>
<body>
  <h4>Create Product</h4>

  <div class="admin-container">
    <form action="" method="post" enctype="multipart/form-data">
      <!-- Product Name -->
      <div class="form-group">
        <label for="nameInput">Product Name</label>
        <input name="name" type="text" class="form-control" id="nameInput" placeholder="Enter Product Name" required />
        <small class="form-text">Enter the name of the product</small>
      </div>
      
      <!-- Price -->
      <div class="form-group">
        <label for="priceInput">Price (PKR)</label>
        <input name="price" type="number" class="form-control" id="priceInput" placeholder="Enter Price" required />
        <small class="form-text">Enter the price of the product</small>
      </div>

      <!-- Product Color -->
      <div class="form-group">
        <label for="colorInput">Product Color</label>
        <input name="color" type="text" class="form-control" id="colorInput" placeholder="Enter Product Color" required />
        <small class="form-text">Specify the color of the product</small>
      </div>

      <!-- Availability -->
      <div class="form-group">
        <label for="availabilityInput">Availability</label>
        <select name="availability" class="form-control" id="availabilityInput">
            <option value="In stock" selected>In stock</option>
            <option value="Out of stock">Out of stock</option>
            <option value="Pre-order">Pre-order</option>
        </select>
        <small class="form-text">Select the product availability</small>
      </div>

      <!-- Description -->
      <div class="form-group">
        <label for="descriptionInput">Description</label>
        <textarea name="description" class="form-control" id="descriptionInput" placeholder="Enter Product Description" rows="3" required></textarea>
        <small class="form-text">Provide details about the product</small>
      </div>

      <!-- Category -->
      <div class="form-group">
        <label for="categoryInput">Category</label>
        <select name="category" class="form-control" id="categoryInput" required>
            <option value="" disabled selected>Select a Category</option>
            <option value="Sofas">Sofas</option>
            <option value="Chairs">Chairs</option>
            <option value="Tables">Tables</option>
        </select>
        <small class="form-text">Select the product category</small>
      </div>

      <!-- Image Upload -->
      <div class="form-group">
        <label for="imageUploadInput">Product Image</label>
        <input name="image" type="file" class="form-control" id="imageUploadInput" required />
        <small class="form-text">Upload an image of the product</small>
      </div>

      <!-- Submit Button -->
      <div class="form-group">
        <button type="submit" class="btn-primary">Create New Product</button>
      </div>
    </form>
  </div>
</body>
</html>
