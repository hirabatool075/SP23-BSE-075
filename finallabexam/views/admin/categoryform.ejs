<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Category</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
        font-family: 'Poppins', sans-serif;
        background-color: #fdf8f0; /* Cream background */
        color: #5a5a5a;
        margin: 0;
        padding: 0;
    }

    h4 {
        text-align: center;
        color: #c62828; /* Deep red for heading */
        font-size: 1.8rem;
        font-weight: bold;
        margin-top: 20px;
    }

    .admin-container {
        max-width: 600px;
        margin: 50px auto;
        padding: 25px 30px;
        background-color: #ffffff; /* White card */
        border: 2px solid #c62828; /* Red border */
        border-radius: 12px;
        box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
        transition: box-shadow 0.3s ease;
    }

    .admin-container:hover {
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    }

    .form-group {
        margin-bottom: 20px;
    }

    .form-group label {
        display: block;
        font-weight: 600;
        margin-bottom: 8px;
        color: #b71c1c; /* Dark red for labels */
        font-size: 1rem;
    }

    .form-control {
        width: 100%;
        padding: 12px 15px;
        border: 1px solid #e57373; /* Light red border */
        border-radius: 8px;
        font-size: 1rem;
        color: #333;
        background-color: #fffdf6; /* Subtle cream background */
        transition: border-color 0.3s ease, box-shadow 0.3s ease;
    }

    .form-control:focus {
        outline: none;
        border-color: #c62828; /* Red focus border */
        box-shadow: 0 0 8px rgba(198, 40, 40, 0.5); /* Red shadow */
        background-color: #ffffff;
    }

    .form-text {
        font-size: 0.85rem;
        color: #757575; /* Muted grey text */
    }

    .btn-primary {
        display: block;
        width: 100%;
        padding: 12px;
        font-size: 1rem;
        font-weight: bold;
        background-color: #c62828; /* Bold red button */
        color: #fff;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .btn-primary:hover {
        background-color: #b71c1c; /* Darker red on hover */
        transform: translateY(-2px);
    }

    .btn-primary:active {
        background-color: #8e0000; /* Even darker red */
        transform: translateY(0);
    }
  </style>
</head>
<body>
  <h4>Add Category</h4>

  <div class="admin-container">
    <form action="/admin/categories/create" method="post">
      <div class="form-group">
        <label for="idInput">ID</label>
        <input 
          name="id" 
          type="text" 
          class="form-control" 
          id="idInput" 
          placeholder="Enter ID" 
          required 
        />
        <small id="idHelp" class="form-text">Category ID</small>
      </div>

      <div class="form-group">
        <label for="titleInput">Title</label>
        <input 
          name="title" 
          type="text" 
          class="form-control" 
          id="titleInput" 
          placeholder="Enter Title" 
          required 
        />
        <small id="titleHelp" class="form-text">Category Title</small>
      </div>

      <div class="form-group">
        <label for="descriptionInput">Description</label>
        <textarea 
          name="description" 
          class="form-control" 
          id="descriptionInput" 
          placeholder="Enter Description" 
          rows="4"
        ></textarea>
        <small id="descriptionHelp" class="form-text">Category Description</small>
      </div>

      <div class="form-group">
        <button type="submit" class="btn-primary">Add New Category</button>
      </div>
    </form>
  </div>
</body>
</html>
