<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product - SOFA STUDIO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #fdf8e1; /* Cream background */
            font-family: 'Poppins', sans-serif;
        }

        h4 {
            color: #c41e3a; /* SOFA STUDIO Red */
            font-family: 'Playfair Display', serif;
            font-weight: bold;
            text-align: center;
            margin-bottom: 20px;
        }

        .form-container {
            background-color: #ffffff; /* White container */
            border: 1px solid #e6d7b2;
            border-radius: 10px;
            padding: 20px;
            max-width: 700px;
            margin: 30px auto;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        label {
            color: #c41e3a; /* SOFA STUDIO Red */
            font-weight: bold;
            font-family: 'Playfair Display', serif;
        }

        .form-control {
            border: 1px solid #e6d7b2; /* Soft border */
            border-radius: 8px;
        }

        .form-control:focus {
            border-color: #c41e3a;
            box-shadow: 0 0 5px rgba(196, 30, 58, 0.3);
        }

        .form-text {
            color: #8c1428; /* Slightly darker red */
        }

        .btn {
            background-color: #c41e3a;
            border: none;
            color: #fff;
            font-weight: bold;
            padding: 10px 20px;
            border-radius: 30px;
            font-family: 'Poppins', sans-serif;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #8c1428; /* Darker red */
        }

        .existing-image {
            margin-top: 10px;
            max-width: 120px;
            border-radius: 5px;
        }

        .form-group {
            margin-bottom: 15px;
        }
    </style>
</head>
<body>

<h4>Edit Product</h4>

<div class="form-container">
    <form action="/admin/products/edit/<%= product._id %>" method="post" enctype="multipart/form-data">
        <!-- Product Name -->
        <div class="form-group">
            <label for="nameInput">Product Name</label>
            <input 
                name="name" 
                type="text" 
                class="form-control" 
                id="nameInput" 
                placeholder="Enter Product Name" 
                value="<%= product.name %>" 
                required 
            />
            <small class="form-text">Product Name</small>
        </div>

        <!-- Product Model -->
        <div class="form-group">
            <label for="modelInput">Product Model</label>
            <input 
                name="model" 
                type="text" 
                class="form-control" 
                id="modelInput" 
                placeholder="Enter Product Model" 
                value="<%= product.model %>" 
                required 
            />
            <small class="form-text">Model (e.g., BM6780-BLUE)</small>
        </div>

        <!-- Price -->
        <div class="form-group">
            <label for="priceInput">Price (PKR)</label>
            <input 
                name="price" 
                type="number" 
                class="form-control" 
                id="priceInput" 
                placeholder="Enter Price" 
                value="<%= product.price %>" 
                required 
            />
            <small class="form-text">Product Price</small>
        </div>

        <!-- Availability -->
        <div class="form-group">
            <label for="availabilityInput">Availability</label>
            <select name="availability" class="form-control" id="availabilityInput">
                <option value="In stock" <%= product.availability === 'In stock' ? 'selected' : '' %>>In stock</option>
                <option value="Out of stock" <%= product.availability === 'Out of stock' ? 'selected' : '' %>>Out of stock</option>
                <option value="Pre-order" <%= product.availability === 'Pre-order' ? 'selected' : '' %>>Pre-order</option>
            </select>
            <small class="form-text">Availability status</small>
        </div>

        <!-- Image Upload -->
        <div class="form-group">
            <label for="imageUploadInput">Product Image</label>
            <input 
                name="image" 
                type="file" 
                class="form-control" 
                id="imageUploadInput" 
            />
            <small class="form-text">Upload a new image to replace the current one.</small>
        </div>

        <% if (product.imageUrls) { %>
        <div class="form-group">
            <label>Existing Image</label>
            <img src="<%= product.imageUrls %>" alt="Product Image" class="existing-image" />
            <small class="form-text">Current product image</small>
        </div>
        <% } %>

        <!-- Description -->
        <div class="form-group">
            <label for="descriptionInput">Product Description</label>
            <textarea 
                name="description" 
                class="form-control" 
                id="descriptionInput" 
                rows="4" 
                placeholder="Enter product description"
            ><%= product.description %></textarea>
            <small class="form-text">Brief description about the product.</small>
        </div>

        <!-- Submit -->
        <div class="form-group text-center">
            <button type="submit" class="btn">Update Product</button>
        </div>
    </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
